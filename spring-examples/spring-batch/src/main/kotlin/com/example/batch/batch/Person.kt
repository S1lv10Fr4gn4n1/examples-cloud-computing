package com.example.batch.batch

// It needs to define defaults to instantiate it with default constructor.
// And the fields need to have set
data class Person(var firstName: String = "", var lastName: String = "")